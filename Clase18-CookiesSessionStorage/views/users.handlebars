<dir>
    {{#each users}}
        <p>{{first_name}}</p>
    {{/each}}

    {{#if hasPrevPage}}
        <a href="/users?page={{prevPage}}"><button>Anterior</button></a>
    {{/if}}

    {{#if hasNextPage}}
        <a href="/users?page={{nextPage}}"><button>Siguiente</button></a>
    {{/if}}


</dir>